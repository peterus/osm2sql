#include <algorithm>
using namespace std;
#include "CreatePoly.h"


vector<Polygon> * CreatePoly(list<OSMRelation * > * pRelations)
{
	vector<Polygon> * pPolys = new vector<Polygon>();

	return pPolys;
}
